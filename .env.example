# Postmark (wysyłka e-maili z formularzy)
POSTMARK_API_TOKEN=
CONTACT_EMAIL=kontakt@hydrobagger.pl

# Analityka
NEXT_PUBLIC_GTM_ID=
NEXT_PUBLIC_GA4_MEASUREMENT_ID=G-KJ0ZJPP8K9

# SEO / sitemap
NEXT_PUBLIC_SITE_URL=https://hydrobagger.pl

# PostgreSQL na VPS (hydrobagger_db) — rola z uprawnieniem tylko INSERT do
# tabeli `leads`. Bez tej zmiennej strona działa normalnie (formularze
# wysyłają tylko e-mail), zapis do panelu admina jest dodatkowy.
DATABASE_URL=postgresql://hydrobagger_site:HASLO@127.0.0.1:5432/hydrobagger_db

# Produkcja na VPS
NODE_ENV=production
PORT=3005
